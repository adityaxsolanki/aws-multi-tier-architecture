pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'docker build -t adityaxsolanki/aws-crime-syndicate:v1 .'
            }
        }
        stage('Push') {
            steps {
                sh 'docker push adityaxsolanki/aws-crime-syndicate:v1'
            }
        }
    }
}
